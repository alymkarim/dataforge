import type { EventRecord } from "../types/pipeline";
export function EventTable({rows}:{rows:EventRecord[]}) {return <div className="table-wrap"><table><thead><tr><th>Time</th><th>Event</th><th>Product</th><th>Category</th><th>Brand</th><th>Price</th><th>User</th></tr></thead><tbody>{rows.map((r,i)=><tr key={`${r.user_session}-${i}`}><td>{r.event_time}</td><td><span className={`event-pill ${r.event_type}`}>{r.event_type}</span></td><td>{r.product_id}</td><td>{r.category_code ?? "—"}</td><td>{r.brand ?? "Unknown"}</td><td>€{r.price.toFixed(2)}</td><td>{r.user_id}</td></tr>)}</tbody></table></div>}