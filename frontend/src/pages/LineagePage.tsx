import { useEffect,useState } from "react";
import { ErrorState } from "../components/ErrorState";import { LineageGraph } from "../components/LineageGraph";import { LoadingState } from "../components/LoadingState";import { PageHeader } from "../components/PageHeader";import { SectionCard } from "../components/SectionCard";import { loadLineage } from "../services/pipelineApi";import type { LineageNode } from "../types/pipeline";
export function LineagePage(){const [nodes,setNodes]=useState<LineageNode[]|null>(null);const [error,setError]=useState("");useEffect(()=>{loadLineage().then(setNodes).catch(e=>setError(e instanceof Error?e.message:"Unable to load lineage"));},[]);if(error)return <ErrorState message={error}/>;if(!nodes)return <LoadingState/>;return <><PageHeader title="Data lineage" description="Trace exactly how source data moves and changes across the platform."/><SectionCard title="End-to-end lineage" subtitle="Every layer exposes its path, row count and major transformations"><LineageGraph nodes={nodes}/></SectionCard></>}
