import { PageHeader } from "../components/PageHeader";import { SectionCard } from "../components/SectionCard";import { PipelineFlow } from "../components/PipelineFlow";import { RunTimeline } from "../components/RunTimeline";import { useDashboardData } from "../hooks/useDashboardData";import { LoadingState } from "../components/LoadingState";import { ErrorState } from "../components/ErrorState";
export function PipelinePage(){const {data,loading,error}=useDashboardData();if(loading)return <LoadingState/>;if(error||!data)return <ErrorState message={error}/>;return <><PageHeader title="Pipeline" description="Bronze–Silver–Gold execution and transformation observability."/><SectionCard title="Current pipeline"><PipelineFlow bronze={data.summary.bronzeRows} silver={data.summary.silverRows} gold={data.summary.goldRows}/></SectionCard><SectionCard title="Recent executions" subtitle="Traceable run-level history"><RunTimeline runs={data.runs}/></SectionCard></>}