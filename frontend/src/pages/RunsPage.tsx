import { Link } from "react-router-dom";import { PageHeader } from "../components/PageHeader";import { SectionCard } from "../components/SectionCard";import { RunTimeline } from "../components/RunTimeline";import { useDashboardData } from "../hooks/useDashboardData";import { LoadingState } from "../components/LoadingState";import { ErrorState } from "../components/ErrorState";
export function RunsPage(){const {data,loading,error}=useDashboardData();if(loading)return <LoadingState/>;if(error||!data)return <ErrorState message={error}/>;return <><PageHeader title="Pipeline runs" description="Execution history for operational traceability and debugging."/><SectionCard title="Execution timeline"><RunTimeline runs={data.runs}/><div className="details-link-wrap"><Link className="details-link" to="/runs/latest">Open latest run details →</Link></div></SectionCard></>}
