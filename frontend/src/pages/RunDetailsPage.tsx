import { useEffect,useState } from "react";import { ErrorState } from "../components/ErrorState";import { LoadingState } from "../components/LoadingState";import { PageHeader } from "../components/PageHeader";import { RunStageBreakdown } from "../components/RunStageBreakdown";import { SectionCard } from "../components/SectionCard";import { loadDetailedRun } from "../services/pipelineApi";import type { DetailedPipelineRun } from "../types/pipeline";
export function RunDetailsPage(){const [run,setRun]=useState<DetailedPipelineRun|null>(null);const [error,setError]=useState("");useEffect(()=>{loadDetailedRun().then(setRun).catch(e=>setError(e instanceof Error?e.message:"Unable to load run"));},[]);if(error)return <ErrorState message={error}/>;if(!run)return <LoadingState/>;return <><PageHeader title="Run details" description={`Operational breakdown for ${run.id}.`}/><SectionCard title="Execution profile" subtitle={`${run.startedAt} → ${run.finishedAt}`}><RunStageBreakdown run={run}/></SectionCard></>}
