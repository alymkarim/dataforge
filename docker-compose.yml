services:
  backend:
    build: .
    container_name: dataforge-backend

    ports:
      - "8000:8000"

    volumes:
      - ./data:/app/data
      - ./configs:/app/configs

    environment:
      - PYTHONPATH=/app/src

    restart: unless-stopped